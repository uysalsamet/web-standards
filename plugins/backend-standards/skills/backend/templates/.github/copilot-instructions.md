# Copilot Instructions

This project's AI instructions live in `AGENTS.md`, the shared source for all tools.
Read it and follow it exactly.

Short summary: the standard under `backend-standards/` is binding.
Go 1.25.12 + Gin v1.12 + pgx/v5, single stack. Before writing code:
`01-GOLDEN-RULES.md` plus `RULE-MAP.md` §1; before finishing:
`tools/check-standards.sh` plus `15-NEW-SERVICE-CHECKLIST.md`.
